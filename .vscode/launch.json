{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Debug using camera",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/main",
            
            "cwd": "${workspaceRoot}"
        },

        {
            "name": "Debug using image (turbine.jpg)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/build/main",
            "args": ["${workspaceRoot}/build/turbine.jpg"],
            "cwd": "${workspaceRoot}"
        }
    ]
}